#include "Widget.hpp"

Widget::Widget(uint16_t width, uint16_t height, uint16_t xOffset, uint16_t yOffset, uint8_t padding) : width(width), height(height), xOffset(xOffset), yOffset(yOffset), padding(padding)
{
}
